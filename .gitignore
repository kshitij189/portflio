# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw screen recordings. These are hundreds of MB and must never be committed —
# GitHub rejects files over 100MB. Drop originals here and encode them to
# compressed .webm in the parent directory instead.
src/assets/videos/raw/
*.mkv
*.mov
